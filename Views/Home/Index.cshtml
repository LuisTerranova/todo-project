@{
    ViewData["Title"] = "Home";
    Layout = "_Layout";
}

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">
        @TempData["SuccessMessage"]
    </div>    
}

<div class="d-flex flex-column justify-content-center align-items-center" style="height: 70vh;">
    <h1 class="display-3 mb-3 text-primary fw-bold">Todo App</h1>
    <p class="lead text-secondary text-center">
        Uma aplicação simples de tarefas criada para fins de estudo e prática em ASP.NET Core MVC.
    </p>
</div>